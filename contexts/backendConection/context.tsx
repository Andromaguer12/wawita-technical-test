/* eslint-disable @typescript-eslint/no-explicit-any */
import React from 'react';

const empty = undefined;

const FetchingContext = React.createContext<any>(empty);

export default FetchingContext;
